How to run the project

First, we need Python installed.
Intsall MySql and run the db.sql script and run the DB server 
Open terminal within the project directory and run pip install virtualenv
In the same terminal pip install -r requirements.txt
Python -m flask run
If everything goes well you should be able to access the website from localhost:5000 
